{
  "name": "admin",
  "private": true,
  "scripts": {
    "dev": "next dev --port 8001",
    "build": "next build",
    "start": "next start",
    "test": "jest --watch",
    "test:ci": "jest --ci"
  }
}